{
  "name": "microservice-template",
  "version": "0.1.0",
  "scripts": {
    "prepare": "husky",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.8",
    "husky": "^9.1.7",
    "typescript": "^5.7.3"
  }
}
